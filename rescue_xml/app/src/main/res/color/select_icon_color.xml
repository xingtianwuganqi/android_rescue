<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 状态为选中时的颜色 -->
    <item android:color="@color/color_system" android:state_checked="true" />
    <!-- 状态为未选中时的颜色 -->
    <item android:color="@color/color_desc" android:state_checked="false" />
</selector>