<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".user.activity.UserChangePasswordActivity">

    <EditText
        android:id="@+id/origin_password"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:background="@null"
        android:padding="5dp"
        android:autofillHints="@string/user_origin_password"
        android:hint="@string/user_origin_password"
        android:textSize="@dimen/content_font"
        android:selectAllOnFocus="true"
        android:layout_gravity="center"
        android:maxLines="1"
        android:theme="@style/EditTextStyle"
        android:gravity="start"
        android:inputType="textPassword"
        android:imeOptions="flagNoEnterAction"
        />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_eee"/>

    <EditText
        android:id="@+id/new_password"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:background="@null"
        android:padding="5dp"
        android:autofillHints="@string/user_new_password"
        android:hint="@string/user_new_password"
        android:textSize="@dimen/content_font"
        android:selectAllOnFocus="true"
        android:layout_gravity="center"
        android:maxLines="1"
        android:theme="@style/EditTextStyle"
        android:gravity="start"
        android:inputType="textPassword"
        android:imeOptions="flagNoEnterAction"
        />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_eee"/>

    <EditText
        android:id="@+id/confirm_password"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:background="@null"
        android:padding="5dp"
        android:autofillHints="@string/user_confirm_password"
        android:hint="@string/user_confirm_password"
        android:textSize="@dimen/content_font"
        android:selectAllOnFocus="true"
        android:layout_gravity="center"
        android:maxLines="1"
        android:theme="@style/EditTextStyle"
        android:gravity="start"
        android:inputType="textPassword"
        android:imeOptions="flagNoEnterAction"
        />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_eee"/>

</LinearLayout>